## things I learn and what I found difficult
I learnt how to link a separate javascript file to my html and how to create a table with javascript. 

I found that swaping the javascript was pretty simple but an issue that I have is that if the width is wider than the view box, if you scroll to the right, the top and bottom rows are no longer highlighted.I fixed this by applying the style on an aside instead of just the div like this: div:nth-child(1) aside
